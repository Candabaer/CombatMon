using UnityEngine;

public class TypeInstance : MonoBehaviour
{
	private Type a;

	public TypeInstance(Type a)
	{
		this.a = a;
	}

	// Start is called once before the first execution of Update after the MonoBehaviour is created
	void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
